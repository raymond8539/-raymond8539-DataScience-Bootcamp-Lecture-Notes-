# SELECT FROM allow you to extract a fraction of the entire database
# example
USE employees;
SELECT first_name , last_name FROM employees;
# you can also use the * to extract all the information in the emplyees database
SELECT * FROM employees;
#Assignment:Select the information from the "dept_no" column of the "departments" table.
#Select all data from the "departments" table
SELECT dept_no
 FROM departments;
SELECT * 
FROM departments;


